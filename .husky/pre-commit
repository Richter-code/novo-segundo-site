#!/bin/sh
# Resolve absolute directory of this hook and source husky helper (POSIX-safe)
DIR="$(dirname "$0")"
HOOK_DIR="$(cd "$DIR" >/dev/null 2>&1 && pwd)"
. "$HOOK_DIR/_/husky.sh"

# Run lint-staged once
npx --no -- lint-staged
