
#!/bin/sh
# Resolve absolute directory of this hook and source husky helper
HOOK_DIR="$(cd "$(dirname \"$0\")" && pwd)"
. "$HOOK_DIR/_/husky.sh"

# Validate commit message
npx --no -- commitlint --edit "$1"
